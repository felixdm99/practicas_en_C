#include <stdio.h>
#define MAX 5
void mostrar(int a[]);
int main()
{
    int vec[MAX];
    int i;
    for ( i = 0; i < MAX; i++)
    {
        printf("ingrese un elemento entero\n");
        scanf("%d",&vec[i]);
    }
    mostrar(vec);
    return 0;
}

void mostrar(int a[])
{
    int i;
    for ( i = 0; i < MAX; i++)
    {
        
    }
    
}
